# Next steps — ordered pickup list

> Template. The ordered to-do to resume with. Make each step self-contained; paste
> exact commands where a human or agent will need to run them.

## 1. <Top step — the first thing to do on resume>
<What, why, and the exact command(s) if any. Note who runs it (agent vs. human)
and any approval gate it crosses.>

## 2. <Next>
<...>

## N. <Lower-priority / optional>
<...>

## Standing constraints (don't violate while doing the above)
<Copy the hard rules from CURRENT_STATE so they travel with the action list —
approval ladders, review/council gates, repo ownership, cloud/prod limits.>
